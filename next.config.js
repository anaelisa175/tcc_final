/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "t3.ftcdn.net", "t4.ftcdn.net"]
  }
};

module.exports = nextConfig;
